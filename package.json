{
  "name": "ts-validator",
  "type": "commonjs",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "private": true,
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist/",
    "test": "npx jest --config jest.config.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.0",
    "ts-node": "^10.9.2"
  }
}
